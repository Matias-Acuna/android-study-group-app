<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <!-- Application configuration starts here -->
    <application
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.DisciteOmnes"
        android:usesCleartextTraffic="true"
        tools:targetApi="31">

        <!-- Screen 5: Task Tracker -->
        <!-- Allows users to track assigned tasks and mark them complete -->
        <activity
            android:name=".TaskTrackerActivity"
            android:exported="false" />

        <!-- Screen 4: Study Plan -->
        <!-- Displays tasks and deadlines within a study group -->
        <activity
            android:name=".StudyPlanActivity"
            android:exported="false" />

        <!-- Screen 3: Group List -->
        <!-- Lists all public groups available to join -->
        <activity
            android:name=".GroupListActivity"
            android:exported="false" />

        <!-- Screen 2: Dashboard -->
        <!-- Home screen showing the user's groups and quick actions -->
        <!-- Create Group screen (from FAB in Dashboard) -->
        <activity
            android:name=".CreateGroupActivity"
            android:exported="false" />
        <activity
            android:name=".DashboardActivity"
            android:exported="false" />

        <!-- Screen 1: Login Activity (Main Entry Point) -->
        <!-- Initial screen shown when the app launches -->
        <activity
            android:name=".LoginActivity"
            android:exported="true"
            android:theme="@style/Theme.DisciteOmnes">

            <!-- This filter makes LoginActivity the launcher (first screen) -->
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

    </application>

</manifest>
